fn main() {
    // Scaffold placeholder. The S9b implementation pipeline replaces this with the
    // contract/v1 one-shot dispatch (describe/schema/policy/launch/terminal/quota/
    // session/rotation/discovery/settings/setup/migration).
    eprintln!("agent-runner-opencode: not yet implemented");
    std::process::exit(64);
}
